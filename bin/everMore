#!/usr/bin/env bash

URL="https://www.last.fm/music/Evermore/Real%20Life"
timeStamps="$(date)"

#read -p "Please enter a link ? URL

lynx --source "$URL" > $HOME/temp/EVERMORE-TRACKS-SRC.txt

awk -F\" '$0 ~ "data-youtube-url"{ print $2}' $HOME/temp/EVERMORE-TRACKS-SRC.txt > $HOME/temp/dLinks.txt

while read -r url 
do 
    #echo "$url"
    mpv -no-video "$url" 
    sleep 0.7 
done <  $HOME/temp/dLinks.txt 




