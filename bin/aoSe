#!/usr/bin/env bash
#
# Usage:
#       aoSe query that you would like to search
#

q=""
i=0

for var in "$@"
do
   if [ "$i" -eq "0" ]
   then
      q+=$var
   else
      q+="+"$var
   fi
   i+=1
done

qutebrowser https://discourse.aosus.org/search?q="$q" &

